<div class="navbar navbar-inverse navbar-static-top" role="navigation">
	<div class="container">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<a class="navbar-brand" href="/">easyMetrics</a>
		</div>
		<div class="collapse navbar-collapse">
			<ul class="nav navbar-nav">
				<li class="<%= 'active' if params[:active] == 'home' %>">
					<a href="/static_pages/home">Home</a>
				</li>
			</ul>
			<% if user_signed_in? %>
			<ul class="nav navbar-nav navbar-right">
				<li class="<%= 'active' if params[:active] == 'sign_up' %>">
					<%= link_to "Sign out", destroy_user_session_path, :method => :delete %>
				</li>
				<%else%>
				<ul class="nav navbar-nav navbar-right">
					<li class="<%= 'active' if params[:active] == 'sign_up' %>">
						<a href="/users/sign_up">Sign up</a>
					</li>
					<li class="<%= 'active' if params[:active] == 'login' %>">
						<a href="#loginModal" role="button" data-toggle="modal">Login</a>
					</li>
				</ul>
				<%end%>
		</div>
		<!--/nav-collapse -->
	</div>
</div>
<!--Login popout -->
<% if !user_signed_in? %>
<%= render 'devise/sessions/new' %>
<%end%>