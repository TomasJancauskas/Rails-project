<div class="navbar navbar-inverse navbar-static-top" role="navigation">
	<div class="container">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<a class="navbar-brand" href="/">easyMetrics</a>
		</div>
		<div class="collapse navbar-collapse">
			<ul class="nav navbar-nav">
				<% if current_class?('/static_pages/home').length > 0%>
				<li class="<%= current_class?('/static_pages/home') %>">
					<%else%>
				<li class="<%= current_class?('/') %>">
					<%end%>
					<a href="/static_pages/home">Home</a>
				</li>

			</ul>
			<ul class="nav navbar-nav navbar-right">
			<% if user_signed_in? %>
				<li><a><%= "Hello, " + current_user.email %></a></li>
			<%end%>
			<% if user_signed_in? %>
			<ul class="nav navbar-nav navbar-right">
				<li>
					<%= link_to "Sign out", destroy_user_session_path, :method => :delete %>
				</li>
				<%else%>
				<ul class="nav navbar-nav navbar-right">
					<li class="<%= current_class?('/users/sign_up')%> <%= current_class?('/users')%>">
						<a href="/users/sign_up">Sign up</a>
					</li>
					<li class="<%= current_class?('/users/sign_in') %>">
						<a href="#loginModal" role="button" data-toggle="modal">Login</a>
					</li>
				</ul>
				<%end%>
		</div>
		<!--/nav-collapse -->
	</div>
</div>
<!--Login popout -->
<% if !user_signed_in? %>
<%= render 'devise/sessions/new' %>
<%end%>